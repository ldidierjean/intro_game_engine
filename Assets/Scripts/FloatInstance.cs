using UnityEngine;

using UnityEngine;

[CreateAssetMenu(fileName = "Float Instance", menuName = "Scriptable instances/Float Instance", order = 1)]
public class FloatInstance : ScriptableObject
{
    public float value;
}